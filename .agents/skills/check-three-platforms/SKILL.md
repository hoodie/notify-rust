---
name: check-three-platforms
description: use cargo aliases in this repo to check that this project builds on windows, macos andl inux
disable-model-invocation: true
---

User `mac-check`, `win-check` and `linux` check to verify that this project currently builds on all three supported platforms
